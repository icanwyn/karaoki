# Optional: server-side Whisper (faster / higher quality than browser model).
# Without this key, Karaoki uses on-device Whisper in the browser automatically.
# Get a key at https://platform.openai.com/api-keys
OPENAI_API_KEY=
